# Base URL of the made-royale-be backend's /api/v1 mount
NEXT_PUBLIC_API_URL=http://localhost:8080/api/v1

# Public site URL — used as metadataBase for resolving Open Graph/Twitter image URLs
NEXT_PUBLIC_SITE_URL=http://localhost:3000
